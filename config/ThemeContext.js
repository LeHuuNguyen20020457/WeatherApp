import { createContext } from 'react';

export const ThemeContext = createContext({});
export const TitleContext = createContext({});
export const DataContext = createContext({});
export const scrollHorizontalContext = createContext({});
